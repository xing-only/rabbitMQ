<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/beans
     http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
     http://www.springframework.org/schema/rabbit
     http://www.springframework.org/schema/rabbit/spring-rabbit-1.0.xsd">

<!-- 配置connection-factory 指定连接rabbit server参数 -->
<rabbit:connection-factory id="connectionFactory" username="guest" password="guest" host="127.0.0.1" port="5672" virtual-host="springhost"/>

<!-- 定义rabbit template 用于数据的接收和发送 -->
<rabbit:template id="rabbitTemplate" connection-factory="connectionFactory" exchange="exchange_spring_xing_direct" message-converter="jsonMessageConverter"/>

<!-- 通过指定下面的admin信息，当前producer中的exchange和 queue 会在 rabbitmq服务器上自动生成 -->
<rabbit:admin connection-factory="connectionFactory" />

<!-- 消息对象json转换类 -->
<bean id="jsonMessageConverter" class="org.springframework.amqp.support.converter.Jackson2JsonMessageConverter" />

<!-- 声明一个消息队列queue
    durable：是否持久化
    auto-delete：当所有消费者客户端连接断开后，是否自动删除队列
    exclusive：仅创建者可以使用的私有队列，断开后自动删除
-->
<rabbit:queue id="q_spring_xing_1" name="q_spring_xing_1" durable="true" auto-delete="false" exclusive="false"/>
<rabbit:queue id="q_spring_xing_2" name="q_spring_xing_2" durable="true" auto-delete="false" exclusive="false"/>

<!--
    rabbit:direct-exchange：定义exchange模式为direct，意思就是消息与一个特定的路由键完全匹配，才会转发。
 -->
<rabbit:direct-exchange name="exchange_spring_xing_direct" id="exchange_spring_xing_direct" auto-delete="false" durable="true">
    <rabbit:bindings>
        <rabbit:binding queue="q_spring_xing_1" key="xing"></rabbit:binding>
        <rabbit:binding queue="q_spring_xing_2" key="kong"></rabbit:binding>
    </rabbit:bindings>
</rabbit:direct-exchange>

<!--
    配置监听：acknowledge="manual" 设置手动应答，当消息处理失败时，会一直重发知道消息处理成功
    queues：队列，当有多个时，用逗号隔开（","）
    ref：监听器，即消费者
 -->
<rabbit:listener-container connection-factory="connectionFactory" acknowledge="manual">
    <rabbit:listener queues="q_spring_xing_1" ref="autoListener"/>
    <rabbit:listener queues="q_spring_xing_2" ref="auto2Listener"/>
</rabbit:listener-container>
</beans>